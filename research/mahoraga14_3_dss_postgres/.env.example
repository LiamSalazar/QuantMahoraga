DSS_BACKEND=parquet
DSS_PROFILE=small
DSS_API_HOST=127.0.0.1
DSS_API_PORT=8002

# Linux local socket, no password stored:
DATABASE_URL=postgresql:///mahoraga_dss

# TCP example. Replace placeholders in your shell, not in committed files:
# DATABASE_URL=postgresql://<user>:<password>@127.0.0.1:5432/mahoraga_dss

VITE_API_BASE=http://127.0.0.1:8002
